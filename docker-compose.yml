services:
  weather-agent:
    image: ghcr.io/OWNER/REPO:latest
    container-name: weather-agent
    restart: unless-stopped
    ports: 
      - "8000:8000"
    environment:
      - GROQ_API_KEY = ${GROQ_API_KEY}